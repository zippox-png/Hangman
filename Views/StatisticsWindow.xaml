<Window x:Class="Hangman.Views.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Statistici" Height="400" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Statistici jucători" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Utilizator" Binding="{Binding Username}" Width="*"/>
                <DataGridTextColumn Header="Categorie" Binding="{Binding Category}" Width="*"/>
                <DataGridTextColumn Header="Jucate" Binding="{Binding Played}" Width="80"/>
                <DataGridTextColumn Header="Câștigate" Binding="{Binding Won}" Width="80"/>
                <DataGridTextColumn Header="Win Rate" Binding="{Binding WinRate}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Închide" HorizontalAlignment="Right"
                Margin="0,8,0,0" Padding="16,6" Click="Close_Click"/>
    </Grid>
</Window>
